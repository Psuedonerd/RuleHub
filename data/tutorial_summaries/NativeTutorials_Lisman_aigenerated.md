# Model Explanation: Lisman

## One-sentence summary

Lisman demonstrates lisman tutorial model.

## What the model shows

Lisman shows lisman tutorial model. It is useful because it keeps attention on lisman starts from the named tutorial molecules and shows how they change over the simulated scenario while still connecting that step to the model readouts.

## Biological story

Lisman is a tutorial model focused on lisman tutorial model. The biological narrative is built around lisman starts from the named tutorial molecules and shows how they change over the simulated scenario, with lisman tracks how k1, p shift between the main states of the example providing the next layer of behavior rather than a generic pathway-wide description.

## Main biological players

K1, P

## Mechanism in plain English

Lisman begins with lisman starts from the named tutorial molecules and shows how they change over the simulated scenario. It then adds lisman tracks how k1, p shift between the main states of the example, which changes how K1, P contribute to the tutorial behavior. The third modeled step, lisman connects the modeled change to a visible readout that can be compared across simulation time, connects the upstream event to the readouts a user would inspect after simulation.

## Key modeled events

- Lisman starts from the named tutorial molecules and shows how they change over the simulated scenario.
- Lisman tracks how K1, P shift between the main states of the example.
- Lisman connects the modeled change to a visible readout that can be compared across simulation time.

## What the model measures

The readouts track K1p. Together, these measurements show how Lisman moves material among its central tutorial states.

## Expected behavior in plots

For Lisman, inspect readouts that report lisman starts from the named tutorial molecules and shows how they change over the simulated scenario and compare them with readouts affected by lisman tracks how k1, p shift between the main states of the example. A visible delay, plateau, or separation between those outputs indicates how lisman connects the modeled change to a visible readout that can be compared across simulation time changes the tutorial dynamics.

## Caveats

Lisman is intentionally instructional, so it focuses on lisman tutorial model and leaves out many biological details that would distract from the tutorial concept.
