# Model Explanation: Repressilator

## One-sentence summary

Repressilator demonstrates repressilator circuit.

## What the model shows

Repressilator shows repressilator circuit. It is useful because it keeps attention on repressilator starts from the named tutorial molecules and shows how they change over the simulated scenario while still connecting that step to the model readouts.

## Biological story

Repressilator is a tutorial model focused on repressilator circuit. The biological narrative is built around repressilator starts from the named tutorial molecules and shows how they change over the simulated scenario, with repressilator tracks how gtetr, gci, glaci, mtetr shift between the main states of the example providing the next layer of behavior rather than a generic pathway-wide description.

## Main biological players

gTetR, gCI, gLacI, mTetR, mCI, mLacI, pTetR, pCI, pLacI

## Mechanism in plain English

Repressilator begins with repressilator starts from the named tutorial molecules and shows how they change over the simulated scenario. It then adds repressilator tracks how gtetr, gci, glaci, mtetr shift between the main states of the example, which changes how gTetR, gCI, gLacI, mTetR, mCI, mLacI contribute to the tutorial behavior. The third modeled step, repressilator connects the modeled change to a visible readout that can be compared across simulation time, connects the upstream event to the readouts a user would inspect after simulation.

## Key modeled events

- Repressilator starts from the named tutorial molecules and shows how they change over the simulated scenario.
- Repressilator tracks how gTetR, gCI, gLacI, mTetR shift between the main states of the example.
- Repressilator connects the modeled change to a visible readout that can be compared across simulation time.

## What the model measures

The readouts track pTetR, pCI, pLacI. Together, these measurements show how Repressilator moves material among its central tutorial states.

## Expected behavior in plots

For Repressilator, inspect readouts that report repressilator starts from the named tutorial molecules and shows how they change over the simulated scenario and compare them with readouts affected by repressilator tracks how gtetr, gci, glaci, mtetr shift between the main states of the example. A visible delay, plateau, or separation between those outputs indicates how repressilator connects the modeled change to a visible readout that can be compared across simulation time changes the tutorial dynamics.

## Caveats

Repressilator is intentionally instructional, so it focuses on repressilator circuit and leaves out many biological details that would distract from the tutorial concept.
