# Model Explanation: motor

## One-sentence summary

motor demonstrates motor protein.

## What the model shows

motor shows motor protein by focusing on here are the functionally defined rate laws which call the functions. The tutorial connects that event to readouts that make the modeled behavior visible.

## Biological story

motor is a validation model centered on motor protein. Its narrative moves from here are the functionally defined rate laws which call the functions to defined to determine the rate of the reactions, so the model teaches a specific mechanism rather than a broad pathway survey.

## Main biological players

CheY, Motor

## Mechanism in plain English

motor uses CheY, Motor as the main actors. First, here are the functionally defined rate laws which call the functions. Next, defined to determine the rate of the reactions. Finally, motor tracks how chey motor redistribute among the tutorial states, which is what links the mechanism to the plotted readouts.

## Key modeled events

- Here are the functionally defined rate laws which call the functions.
- Defined to determine the rate of the reactions.
- Motor tracks how CheY Motor redistribute among the tutorial states.

## What the model measures

The readouts track CheYp, MotCW, MotCCW. These measurements are useful for seeing whether here are the functionally defined rate laws which call the functions changes the amount of free, bound, active, inactive, product-like, or complexed material.

## Expected behavior in plots

For motor, compare readouts connected to here are the functionally defined rate laws which call the functions with readouts connected to defined to determine the rate of the reactions. Separation between those curves indicates that the tutorial mechanism is changing the system in stages rather than all at once.

## Caveats

motor is scoped to motor protein. It should be read as a focused tutorial or validation example, not as a comprehensive biological pathway model.
