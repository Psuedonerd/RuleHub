# Model Explanation: LR

## One-sentence summary

LR demonstrates lr tutorial model.

## What the model shows

LR shows lr tutorial model. It is useful because it keeps attention on lr starts from the named tutorial molecules and shows how they change over the simulated scenario while still connecting that step to the model readouts.

## Biological story

LR is a tutorial model focused on lr tutorial model. The biological narrative is built around lr starts from the named tutorial molecules and shows how they change over the simulated scenario, with lr tracks how l, r shift between the main states of the example providing the next layer of behavior rather than a generic pathway-wide description.

## Main biological players

L, R

## Mechanism in plain English

LR begins with lr starts from the named tutorial molecules and shows how they change over the simulated scenario. It then adds lr tracks how l, r shift between the main states of the example, which changes how L, R contribute to the tutorial behavior. The third modeled step, lr connects the modeled change to a visible readout that can be compared across simulation time, connects the upstream event to the readouts a user would inspect after simulation.

## Key modeled events

- LR starts from the named tutorial molecules and shows how they change over the simulated scenario.
- LR tracks how L, R shift between the main states of the example.
- LR connects the modeled change to a visible readout that can be compared across simulation time.

## What the model measures

The readouts track FreeR, Bound. Together, these measurements show how LR moves material among its central tutorial states.

## Expected behavior in plots

For LR, inspect readouts that report lr starts from the named tutorial molecules and shows how they change over the simulated scenario and compare them with readouts affected by lr tracks how l, r shift between the main states of the example. A visible delay, plateau, or separation between those outputs indicates how lr connects the modeled change to a visible readout that can be compared across simulation time changes the tutorial dynamics.

## Caveats

LR is intentionally instructional, so it focuses on lr tutorial model and leaves out many biological details that would distract from the tutorial concept.
