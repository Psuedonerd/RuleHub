# Model Explanation: localfunc

## One-sentence summary

localfunc demonstrates test local function expansion.

## What the model shows

localfunc shows test local function expansion by focusing on a synthesizes c with rate dependent on bound b. The tutorial connects that event to readouts that make the modeled behavior visible.

## Biological story

localfunc is a validation model centered on test local function expansion. Its narrative moves from a synthesizes c with rate dependent on bound b to a binds b, so the model teaches a specific mechanism rather than a broad pathway survey.

## Main biological players

A, B, C, Trash

## Mechanism in plain English

localfunc uses A, B, C, Trash as the main actors. First, a synthesizes c with rate dependent on bound b. Next, a binds b. Finally, degradation of c, which is what links the mechanism to the plotted readouts.

## Key modeled events

- A synthesizes C with rate dependent on bound B.
- A binds B.
- Degradation of C.

## What the model measures

The readouts track Atot, Btot, Ctot, AB0, AB1, AB2, AB3, AB motif. These measurements are useful for seeing whether a synthesizes c with rate dependent on bound b changes the amount of free, bound, active, inactive, product-like, or complexed material.

## Expected behavior in plots

For localfunc, compare readouts connected to a synthesizes c with rate dependent on bound b with readouts connected to a binds b. Separation between those curves indicates that the tutorial mechanism is changing the system in stages rather than all at once.

## Caveats

localfunc is scoped to test local function expansion. It should be read as a focused tutorial or validation example, not as a comprehensive biological pathway model.
