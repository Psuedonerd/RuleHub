# Model Explanation: tlmr

## One-sentence summary

Trivalent ligand monovalent receptor.

## What the model shows

tlmr shows trivalent ligand monovalent receptor. It is useful because it keeps attention on tlmr starts from the named tutorial molecules and shows how they change over the simulated scenario while still connecting that step to the model readouts.

## Biological story

tlmr is a validation model focused on trivalent ligand monovalent receptor. The biological narrative is built around tlmr starts from the named tutorial molecules and shows how they change over the simulated scenario, with tlmr tracks how l, r shift between the main states of the example providing the next layer of behavior rather than a generic pathway-wide description.

## Main biological players

L, R

## Mechanism in plain English

tlmr begins with tlmr starts from the named tutorial molecules and shows how they change over the simulated scenario. It then adds tlmr tracks how l, r shift between the main states of the example, which changes how L, R contribute to the tutorial behavior. The third modeled step, tlmr connects the modeled change to a visible readout that can be compared across simulation time, connects the upstream event to the readouts a user would inspect after simulation.

## Key modeled events

- tlmr starts from the named tutorial molecules and shows how they change over the simulated scenario.
- tlmr tracks how L, R shift between the main states of the example.
- tlmr connects the modeled change to a visible readout that can be compared across simulation time.

## What the model measures

The readouts focus on the main tutorial players, especially L, R, so the plotted behavior can be tied back to the mechanism described above.

## Expected behavior in plots

For tlmr, inspect readouts that report tlmr starts from the named tutorial molecules and shows how they change over the simulated scenario and compare them with readouts affected by tlmr tracks how l, r shift between the main states of the example. A visible delay, plateau, or separation between those outputs indicates how tlmr connects the modeled change to a visible readout that can be compared across simulation time changes the tutorial dynamics.

## Caveats

tlmr is primarily a validation-style example for trivalent ligand monovalent receptor; it should be used to understand that encoded behavior rather than as a complete biological pathway.
