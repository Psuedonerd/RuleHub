# Model Explanation: ComplexDegradation

## One-sentence summary

ComplexDegradation demonstrates degradation model.

## What the model shows

ComplexDegradation shows degradation model by focusing on degrades all species in which a is present. The tutorial connects that event to readouts that make the modeled behavior visible.

## Biological story

ComplexDegradation is a tutorial model centered on degradation model. Its narrative moves from degrades all species in which a is present to degrades only a molecule in species where a is present, so the model teaches a specific mechanism rather than a broad pathway survey.

## Main biological players

A, B, C

## Mechanism in plain English

ComplexDegradation uses A, B, C as the main actors. First, degrades all species in which a is present. Next, degrades only a molecule in species where a is present. Finally, degrades b when complexed with a but only if fragment containing b, which is what links the mechanism to the plotted readouts.

## Key modeled events

- Degrades all species in which A is present.
- Degrades only A molecule in species where A is present.
- Degrades B when complexed with A but only if fragment containing B.

## What the model measures

The readouts track the named tutorial outputs. These measurements are useful for seeing whether degrades all species in which a is present changes the amount of free, bound, active, inactive, product-like, or complexed material.

## Expected behavior in plots

For ComplexDegradation, compare readouts connected to degrades all species in which a is present with readouts connected to degrades only a molecule in species where a is present. Separation between those curves indicates that the tutorial mechanism is changing the system in stages rather than all at once.

## Caveats

ComplexDegradation is scoped to degradation model. It should be read as a focused tutorial or validation example, not as a comprehensive biological pathway model.
