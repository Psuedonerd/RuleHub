# Model Explanation: polymer_fixed

## One-sentence summary

Runtime only model model migrated from public/models: polymer fixed.

## What the model shows

polymer_fixed shows runtime only model model migrated from public/models: polymer fixed. It is useful because it keeps attention on a binds b via b1 a sites while still connecting that step to the model readouts.

## Biological story

polymer_fixed is a other model focused on runtime only model model migrated from public/models: polymer fixed. The biological narrative is built around a binds b via b1 a sites, with a binds b via b2 a sites providing the next layer of behavior rather than a generic pathway-wide description.

## Main biological players

A, B, C

## Mechanism in plain English

polymer_fixed begins with a binds b via b1 a sites. It then adds a binds b via b2 a sites, which changes how A, B, C contribute to the tutorial behavior. The third modeled step, a binds c via c a sites, connects the upstream event to the readouts a user would inspect after simulation.

## Key modeled events

- A binds B via b1 a sites.
- A binds B via b2 a sites.
- A binds C via c a sites.

## What the model measures

The readouts track FullyBoundA, FreeA, FreeB, FreeC, LargeClusters. Together, these measurements show how polymer_fixed moves material among its central tutorial states.

## Expected behavior in plots

For polymer_fixed, inspect readouts that report a binds b via b1 a sites and compare them with readouts affected by a binds b via b2 a sites. A visible delay, plateau, or separation between those outputs indicates how a binds c via c a sites changes the tutorial dynamics.

## Caveats

polymer_fixed should be interpreted within its other scope: it explains runtime only model model migrated from public/models: polymer fixed, not every possible interaction involving A, B, C.
