# Model Explanation: polymer_fixed

## One-sentence summary

polymer_fixed demonstrates polymer fixed tutorial model.

## What the model shows

polymer_fixed shows polymer fixed tutorial model by focusing on a binds b via b1 a sites. The tutorial connects that event to readouts that make the modeled behavior visible.

## Biological story

polymer_fixed is a other model centered on polymer fixed tutorial model. Its narrative moves from a binds b via b1 a sites to a binds b via b2 a sites, so the model teaches a specific mechanism rather than a broad pathway survey.

## Main biological players

A, B, C

## Mechanism in plain English

polymer_fixed uses A, B, C as the main actors. First, a binds b via b1 a sites. Next, a binds b via b2 a sites. Finally, a binds c via c a sites, which is what links the mechanism to the plotted readouts.

## Key modeled events

- A binds B via b1 a sites.
- A binds B via b2 a sites.
- A binds C via c a sites.

## What the model measures

The readouts track FullyBoundA, FreeA, FreeB, FreeC, LargeClusters. These measurements are useful for seeing whether a binds b via b1 a sites changes the amount of free, bound, active, inactive, product-like, or complexed material.

## Expected behavior in plots

For polymer_fixed, compare readouts connected to a binds b via b1 a sites with readouts connected to a binds b via b2 a sites. Separation between those curves indicates that the tutorial mechanism is changing the system in stages rather than all at once.

## Caveats

polymer_fixed is scoped to polymer fixed tutorial model. It should be read as a focused tutorial or validation example, not as a comprehensive biological pathway model.
