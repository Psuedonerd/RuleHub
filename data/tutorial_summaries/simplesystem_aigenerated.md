# Model Explanation: simple_system

## One-sentence summary

simple_system demonstrates simple binding system.

## What the model shows

simple_system shows simple binding system by focusing on simple system tracks how x y redistribute among the tutorial states. The tutorial connects that event to readouts that make the modeled behavior visible.

## Biological story

simple_system is a validation model centered on simple binding system. Its narrative moves from simple system tracks how x y redistribute among the tutorial states to simple_system compares the main players x, y as they become more or less abundant, so the model teaches a specific mechanism rather than a broad pathway survey.

## Main biological players

X, Y

## Mechanism in plain English

simple_system uses X, Y as the main actors. First, simple system tracks how x y redistribute among the tutorial states. Next, simple_system compares the main players x, y as they become more or less abundant. Finally, simple_system connects its central mechanism to the named readouts x free, x p total, xp free, xy, which is what links the mechanism to the plotted readouts.

## Key modeled events

- Simple system tracks how X Y redistribute among the tutorial states.
- simple_system compares the main players X, Y as they become more or less abundant.
- simple_system connects its central mechanism to the named readouts X free, X p total, Xp free, XY.

## What the model measures

The readouts track X free, X p total, Xp free, XY, Ytotal, Xtotal. These measurements are useful for seeing whether simple system tracks how x y redistribute among the tutorial states changes the amount of free, bound, active, inactive, product-like, or complexed material.

## Expected behavior in plots

For simple_system, compare readouts connected to simple system tracks how x y redistribute among the tutorial states with readouts connected to simple_system compares the main players x, y as they become more or less abundant. Separation between those curves indicates that the tutorial mechanism is changing the system in stages rather than all at once.

## Caveats

simple_system is scoped to simple binding system. It should be read as a focused tutorial or validation example, not as a comprehensive biological pathway model.
