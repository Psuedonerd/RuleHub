# Model Explanation: rec_dim

## One-sentence summary

rec_dim demonstrates ligand receptor binding.

## What the model shows

rec_dim shows ligand receptor binding. It is useful because it keeps attention on ligand receptor binding while still connecting that step to the model readouts.

## Biological story

rec_dim is a validation model focused on ligand receptor binding. The biological narrative is built around ligand receptor binding, with rec dim compares how molecular binding creates and releases complexes providing the next layer of behavior rather than a generic pathway-wide description.

## Main biological players

Lig, Rec

## Mechanism in plain English

rec_dim begins with ligand receptor binding. It then adds rec dim compares how molecular binding creates and releases complexes, which changes how Lig, Rec contribute to the tutorial behavior. The third modeled step, rec_dim connects the modeled change to a visible readout that can be compared across simulation time, connects the upstream event to the readouts a user would inspect after simulation.

## Key modeled events

- Ligand receptor binding.
- Rec dim compares how molecular binding creates and releases complexes.
- rec_dim connects the modeled change to a visible readout that can be compared across simulation time.

## What the model measures

The readouts track RecFree, RecBound, Dimers. Together, these measurements show how rec_dim moves material among its central tutorial states.

## Expected behavior in plots

For rec_dim, inspect readouts that report ligand receptor binding and compare them with readouts affected by rec dim compares how molecular binding creates and releases complexes. A visible delay, plateau, or separation between those outputs indicates how rec_dim connects the modeled change to a visible readout that can be compared across simulation time changes the tutorial dynamics.

## Caveats

rec_dim is primarily a validation-style example for ligand receptor binding; it should be used to understand that encoded behavior rather than as a complete biological pathway.
