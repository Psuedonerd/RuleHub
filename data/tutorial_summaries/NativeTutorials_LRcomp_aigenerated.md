# Model Explanation: LR comp

## One-sentence summary

LR comp tutorial model.

## What the model shows

LR comp shows lr comp tutorial model. It is useful because it keeps attention on lr comp starts from the named tutorial molecules and shows how they change over the simulated scenario while still connecting that step to the model readouts.

## Biological story

LR comp is a tutorial model focused on lr comp tutorial model. The biological narrative is built around lr comp starts from the named tutorial molecules and shows how they change over the simulated scenario, with lr comp tracks how l, r shift between the main states of the example providing the next layer of behavior rather than a generic pathway-wide description.

## Main biological players

L, R

## Mechanism in plain English

LR comp begins with lr comp starts from the named tutorial molecules and shows how they change over the simulated scenario. It then adds lr comp tracks how l, r shift between the main states of the example, which changes how L, R contribute to the tutorial behavior. The third modeled step, lr comp connects the modeled change to a visible readout that can be compared across simulation time, connects the upstream event to the readouts a user would inspect after simulation.

## Key modeled events

- LR comp starts from the named tutorial molecules and shows how they change over the simulated scenario.
- LR comp tracks how L, R shift between the main states of the example.
- LR comp connects the modeled change to a visible readout that can be compared across simulation time.

## What the model measures

The readouts track FreeR, Bound, test. Together, these measurements show how LR comp moves material among its central tutorial states.

## Expected behavior in plots

For LR comp, inspect readouts that report lr comp starts from the named tutorial molecules and shows how they change over the simulated scenario and compare them with readouts affected by lr comp tracks how l, r shift between the main states of the example. A visible delay, plateau, or separation between those outputs indicates how lr comp connects the modeled change to a visible readout that can be compared across simulation time changes the tutorial dynamics.

## Caveats

LR comp is intentionally instructional, so it focuses on lr comp tutorial model and leaves out many biological details that would distract from the tutorial concept.
