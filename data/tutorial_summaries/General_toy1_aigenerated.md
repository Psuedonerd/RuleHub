# Model Explanation: toy1

## One-sentence summary

toy1 demonstrates basic signaling toy.

## What the model shows

toy1 shows basic signaling toy by focusing on adaptor binding phosphotyrosine. The tutorial connects that event to readouts that make the modeled behavior visible.

## Biological story

toy1 is a tutorial model centered on basic signaling toy. Its narrative moves from adaptor binding phosphotyrosine to toy1 tracks a compact set of tutorial states and outputs, so the model teaches a specific mechanism rather than a broad pathway survey.

## Main biological players

The named species in toy1

## Mechanism in plain English

toy1 uses The named species in toy1 as the main actors. First, adaptor binding phosphotyrosine. Next, toy1 tracks a compact set of tutorial states and outputs. Finally, toy1 connects its central mechanism to the named readouts r dim, r t1, r t2, r t3, which is what links the mechanism to the plotted readouts.

## Key modeled events

- Adaptor binding phosphotyrosine.
- Toy1 tracks a compact set of tutorial states and outputs.
- toy1 connects its central mechanism to the named readouts R dim, R t1, R t2, R t3.

## What the model measures

The readouts track R dim, R t1, R t2, R t3, R t3a, A R, A tot, R tot. These measurements are useful for seeing whether adaptor binding phosphotyrosine changes the amount of free, bound, active, inactive, product-like, or complexed material.

## Expected behavior in plots

For toy1, compare readouts connected to adaptor binding phosphotyrosine with readouts connected to toy1 tracks a compact set of tutorial states and outputs. Separation between those curves indicates that the tutorial mechanism is changing the system in stages rather than all at once.

## Caveats

toy1 is scoped to basic signaling toy. It should be read as a focused tutorial or validation example, not as a comprehensive biological pathway model.
